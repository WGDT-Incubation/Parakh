# --- Core Image, PDF & OCR Processing ---
pdfplumber
PyMuPDF
Pillow
pytesseract
opencv-python-headless
numpy>=1.26.0
pandas>=2.0.0
fpdf2
rapidfuzz
pdf2image
pathlib
tqdm

# --- Face Detection & Analysis (InsightFace) ---
insightface>=0.7.3
onnxruntime>=1.16.0
# If running locally (not cloud), you can replace the above opencv-python-headless with opencv-python

# --- Optional: Traditional Face recognition (comment out if not needed) ---
# face-recognition     # requires dlib compilation; skip for Streamlit Cloud

# --- NLP / Text Similarity / Processing ---
spacy
scikit-learn
sentence-transformers

# --- Web / App Interface ---
streamlit
streamlit-option-menu

# --- Azure Cognitive Services (for Document / Face / Vision AI) ---
azure-ai-formrecognizer
azure-cognitiveservices-vision-face
azure-core

# --- Utility / File Handling ---
python-magic
requests
faiss-cpu          # correct package name for FAISS (not 'faiss')

# --- For ML / Deep Learning (Optional but useful for enhancement) ---
torch
transformers
